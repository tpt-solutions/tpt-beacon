{
  "name": "tpt-beacon",
  "private": true,
  "version": "0.1.0",
  "license": "MIT OR Apache-2.0",
  "description": "TPT Beacon — self-hosted BI/analytics platform for TPT Keystone",
  "repository": {
    "type": "git",
    "url": "https://github.com/tpt-solutions/tpt-beacon"
  },
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
